// https://qoj.ac/contest/2574/problem/14813
#include<bits/stdc++.h>
using namespace std;
#define int long long

signed main() {
    ios::sync_with_stdio(0);
    cin.tie(0),cout.tie(0);

    struct P
    {
        int x;
        int y;
    };
    

    auto solve = [&]() {
        int n;
        cin>>n;
        vector<P> a(n);
        int sumx=0,sumy=0;
        for(int i=0;i<n;i++){
            cin>>a[i].x>>a[i].y;
            sumx+=a[i].x,sumy+=a[i].y;
        }






























































































































































































































































































































































































































































































        
        vector<int> jc(200005);
        jc[0]=1;
        for(int i=1;i<=100002;i++)jc[i]=jc[i-1]*i;

    };

    solve();
    return 0;
}