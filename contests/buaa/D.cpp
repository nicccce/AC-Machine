#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdbool.h>
#include <math.h>
#include <limits.h>
#define max(a, b) ((a) > (b) ? (a) : (b))
#define min(a, b) ((a) < (b) ? (a) : (b))
#define int long long
void solve(){
    printf("H有点太偏了，要是直接返回WA，我应该能更快想出来");
}
signed main() {
    int t=1;
    // scanf("%d",&t);
    for(;t;t--)solve();
    return 0;
}