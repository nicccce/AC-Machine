#include<bits/stdc++.h>
using namespace std;
#define int long long

signed main() {
    ios::sync_with_stdio(0);
    cin.tie(0),cout.tie(0);

    auto solve = [&]() {
        int d;
        cin>>d;
        cout<<(d)%7+1;
    };
    // int t;
    // for(cin>>t;t;t--)
    solve();
    return 0;
}